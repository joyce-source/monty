#include "monty.h"
/**
  *_nop - opcode that does nothing
  *@head: pointer to the stack
  *@line_number: number on the monty file
  */

void _nop(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
}
